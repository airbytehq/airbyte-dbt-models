version: 2

models:
  - name: tag
    description: This model maps the tag data from Airbyte to Fivetran's format.
    columns:
      - name: tag_id
        description: The unique identifier for the tag.
      - name: name
        description: The name of the tag.