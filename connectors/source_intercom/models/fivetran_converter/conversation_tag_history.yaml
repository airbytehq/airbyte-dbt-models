version: 2

models:
  - name: conversation_tag_history
    description: This model maps the conversation tag history from Airbyte to Fivetran's format.
    columns:
      - name: conversation_id
        description: The unique identifier for the conversation
      - name: tag_id
        description: The unique identifier of the tag associated with the conversation