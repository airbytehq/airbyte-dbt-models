version: 2

models:
  - name: team_admin
    description: This model maps the admin ID and name.
    columns:
      - name: team_id
        description: Team ID
      - name: admin_id
        description: Admin ID
